const {
    // Collection,
    Client,
    // Formatters,
    MessageActionRow,
    MessageButton,
    MessageEmbed,
    MessageSelectMenu,
} = require('discord.js');
module.exports = {
    name: 'fun',
    description: '遊戲系列',
    options: [],
    ignoreFile: true,
    run: async (client, interaction, container) => {
        // 內容
    },
};